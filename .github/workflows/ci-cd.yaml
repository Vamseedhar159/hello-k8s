name: Hello-K8s CI/CD

on:
  push:
    branches:
      - main

jobs:

  build:
    runs-on: ubuntu-latest
    outputs:
      image: ${{ steps.set-image.outputs.image }}
    steps:
      - uses: actions/checkout@v3

      - name: Build Docker image
        id: set-image
        run: |
          IMAGE_NAME=hello-k8s:1.0
          docker build -t $IMAGE_NAME ./app
          echo "image=$IMAGE_NAME" >> $GITHUB_OUTPUT

      - name: Docker login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Push Docker image
        run: |
          docker tag ${{ steps.set-image.outputs.image }} ${{ secrets.DOCKER_USERNAME }}/hello-k8s:1.0
          docker push ${{ secrets.DOCKER_USERNAME }}/hello-k8s:1.0

  smoke-test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v3

      # Step 1: Install kind manually
      - name: Install kind
        run: |
          curl -Lo ./kind https://kind.sigs.k8s.io/dl/v0.20.0/kind-linux-amd64
          chmod +x ./kind
          sudo mv ./kind /usr/local/bin/kind

      # Step 2: Create kind cluster
      - name: Create kind cluster
        run: kind create cluster --name test-cluster

      # Step 3: Docker login & pull image from DockerHub
      - name: Docker login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Pull Docker image
        run: docker pull ${{ secrets.DOCKER_USERNAME }}/hello-k8s:1.0

      # Step 4: Load Docker image into kind
      - name: Load Docker image into kind
        run: kind load docker-image ${{ secrets.DOCKER_USERNAME }}/hello-k8s:1.0

      # Step 5: Setup kubectl
      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'v1.27.3'

      # Step 6: Deploy to test namespace
      - name: Deploy to test namespace
        run: |
          kubectl create ns test --dry-run=client -o yaml | kubectl apply -f -
          kubectl apply -f k8s-deployment.yaml -n test
          kubectl wait --for=condition=ready pod -l app=hello -n test --timeout=60s

      # Step 7: Smoke Test
      - name: Smoke Test
        run: |
          POD_NAME=$(kubectl get pod -l app=hello -n test -o jsonpath='{.items[0].metadata.name}')
          kubectl port-forward $POD_NAME 5000:5000 -n test &
          sleep 5
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:5000/hello)
          if [ "$STATUS" != "200" ]; then
            echo "Smoke test failed!"
            exit 1
          else
            echo "Smoke test passed!"
          fi

  deploy:
    runs-on: ubuntu-latest
    needs: smoke-test
    steps:
      - uses: actions/checkout@v3

      # Demo deploy step
      - name: Deploy to production
        run: echo "Smoke test passed → deploy step would go here"
